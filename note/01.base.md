## cache line

现代 cpu 是按块来读取。再访问连续内存时；对缓存很友好。
但访问离散内存时；会出现 cache miss。又导致cpu不断反复加载数据，性能瓶颈就在这儿了。

## cache miss

同上

## padding

c/c++ 中结构体的内存填充。现代cpu适合整块整块对内存操作。而不是杂乱无序

## alignof

对齐字节大小

## vector vs list

连续/离散

## move / copy

move 移动语义。类似于转交资源。不复制。
copy 完整的复制一次，在有些场景中；没必要持有2份。就使用mvoe
